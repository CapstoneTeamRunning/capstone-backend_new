# Capstone Backend 작업 내역

## 프로젝트 개요

수능 영어 학습 지원 플랫폼 백엔드.

- **스택**: FastAPI + PostgreSQL + Google Gemini / OpenRouter AI + Modal vLLM
- **팀플 프로젝트** — EC2 배포 전 반드시 팀원 공지 및 PR 리뷰 필요
- **현재 브랜치**: `hot-fix`

---

## 발견한 버그

모의고사 빈칸 문제를 주제추론 등 다른 유형으로 변형 생성할 때,
원본 지문의 `______` 빈칸이 채워지지 않은 채 Modal AI에 넘어가
생성된 문제에도 빈칸이 그대로 노출되는 버그.

**원인**: `question_generation.py` → `build_modal_request()` 에
`passage=ocr_item.source_text` 를 그대로 전달하는데,
원본이 빈칸 문제이면 지문에 `______` 가 남아있음.

---

## 해결 방법

### 신규 파일: `app/services/passage_refiner.py`

수능 문제 유형별로 지문을 정제하는 서비스.

```python
refine_passage(passage, *, category_name, choices, answer) -> str
```

| 유형 | 처리 방식 |
|---|---|
| 빈칸 추론 | `______` → `choices[answer-1].text` 로 교체 |
| 문장 삽입 | `[제시문]` 문장을 `( ① )~( ⑤ )` 중 answer 위치에 삽입, 나머지 마커 제거 |
| 글의 순서 | `(A)(B)(C)` 블록을 정답 순서로 재조합 (블록 없으면 [제시문] intro만 사용) |
| 무관한 문장 | answer 번호 문장 제거 + 나머지 번호 마커 제거 |
| 요약문 완성 | `[본문]`만 추출, `[제시문]` 요약 빈칸 제외 |
| 어법 / 지칭 추론 | `①②③④⑤` 인라인 마커 제거 |
| 기타 (주제/제목/요지 등) | `[본문]` / `[제시문]` 레이블만 제거 |

**검증 결과**: 전체 16개 유형 중 15개 ✅  
(도표 이해만 ⚠️ → 이미 `_is_visible_question()`으로 생성 대상에서 제외됨)

---

### `app/api/question_generation.py` 에 추가할 코드

`generate_question_for_exam_question()` 함수 안에서
`extract_first_ocr_item()` 호출 후, `build_modal_request()` 호출 전에 삽입:

```python
from dataclasses import replace as _dataclass_replace
from app.services.passage_refiner import refine_passage

# --- 지문 정제 ---
raw_category_name = str((exam_item.get("category") or {}).get("name") or "")
raw_choices = list((exam_item.get("content") or {}).get("choices") or [])
raw_answer = exam_item.get("answer")
refined = refine_passage(
    ocr_item.passage,
    category_name=raw_category_name,
    choices=raw_choices,
    answer=raw_answer,
)
if refined != ocr_item.passage:
    print(f"[generate] passage refined category={raw_category_name}", flush=True)
    ocr_item = _dataclass_replace(ocr_item, passage=refined, source_text=refined)
# -----------------
```

### 추가할 API 엔드포인트

`question_generation.py` 에 `POST /questions/refine-passage` 추가:

```python
class RefinePassageRequest(BaseModel):
    passage: str
    category_name: str = ""
    choices: list[Any] | None = None
    answer: int | None = None

@router.post("/questions/refine-passage")
def refine_passage_endpoint(body: RefinePassageRequest) -> dict[str, Any]:
    refined = refine_passage(
        body.passage,
        category_name=body.category_name,
        choices=body.choices,
        answer=body.answer,
    )
    return {"refined_passage": refined, "changed": refined != body.passage}
```

---

## 현재 상태

| 항목 | 상태 |
|---|---|
| `app/services/passage_refiner.py` | ✅ 로컬 완성, git untracked (미커밋) |
| `app/api/question_generation.py` | ❌ 원본 상태 (아직 수정 안 함) |
| EC2 서버 | ✅ 원본 상태 (팀 공지 전 배포 취소) |
| 로컬 서버 테스트 | ❌ 미완료 (SSH 터널 연결 필요) |

---

## 다음 할 일

1. **로컬 테스트**
   - SSH 터널로 EC2 DB 연결
   - 로컬에서 uvicorn 실행
   - `http://localhost:8000/docs` Swagger로 `refine-passage` API 테스트
   - 실제 빈칸 문제 변형 생성 흐름 확인

2. **question_generation.py 수정** (위 코드 삽입)

3. **팀원 공지 후 PR 올리기**
   - 앱 담당자: 수정 없음, 신규 생성 문제는 자동으로 정제된 지문 제공됨
   - 백엔드 담당자: `passage_refiner.py` 추가, `question_generation.py` 수정

4. **PR 승인 후 EC2 배포**

---

## EC2 서버 정보

```
주소: ec2-user@3.36.66.188
키: capstone_key.pem (프로젝트 루트)
```

**배포 명령어:**
```bash
# 파일 업로드
scp -i capstone_key.pem app/services/passage_refiner.py \
  ec2-user@3.36.66.188:/home/ec2-user/capstone-server/backend/app/services/passage_refiner.py

scp -i capstone_key.pem app/api/question_generation.py \
  ec2-user@3.36.66.188:/home/ec2-user/capstone-server/backend/app/api/question_generation.py

# 재빌드 & 재시작
ssh -i capstone_key.pem ec2-user@3.36.66.188 \
  "cd /home/ec2-user/capstone-server && docker compose build backend && docker compose up -d backend"
```

**로컬 테스트 SSH 터널:**
```powershell
# 터미널 1 (열어두기)
ssh -i "전체경로\capstone_key.pem" -L 5432:172.18.0.2:5432 -N ec2-user@3.36.66.188

# 터미널 2 (프로젝트 폴더에서)
$env:DB_HOST="localhost"; $env:DB_PORT="5432"; $env:DB_NAME="capstone"
$env:DB_USER="capstone_user"; $env:DB_PASSWORD="capstone_1234"
$env:AI_PROVIDER="openrouter"; $env:AI_MODEL="google/gemini-3.5-flash"
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

> ⚠️ 경로에 한글(`문서`)이 포함되어 있어 PowerShell 스크립트 파일로 실행 시 인코딩 오류 발생.
> 명령어를 터미널에 직접 입력하는 것을 권장.
